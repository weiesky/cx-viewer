# list_mcp_resource_templates

`list_mcp_resource_templates` lists parameterized resource templates exposed by MCP servers. Templates are useful for dynamic context such as a file, record, or business object selected by parameters.

Fields:

- `server`: optional MCP server filter.
- `cursor`: optional pagination cursor.
