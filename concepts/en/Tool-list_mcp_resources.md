# list_mcp_resources

`list_mcp_resources` lists resources provided by MCP servers. Resources can supply files, database schemas, or application-specific context, and should be preferred over web search when available.

Fields:

- `server`: optional MCP server filter.
- `cursor`: optional pagination cursor.
