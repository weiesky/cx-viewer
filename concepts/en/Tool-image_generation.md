# image_generation

`image_generation` generates PNG images. It is the currently loaded image-generation capability, not a legacy file-reading or editing tool.

Use it for bitmap visual assets such as illustrations, textures, sprites, mockups, and transparent-background cutouts.
