# view_image

`view_image` displays a local image file for visual inspection, screenshot review, or asset verification.

Fields:

- `path`: local image path, required.
- `detail`: optional detail level, `high` or `original`.

Legacy `ImageView` and `imageView` entries map to this capability.
