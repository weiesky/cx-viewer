# read_mcp_resource

`read_mcp_resource` reads a concrete resource from an MCP server.

Fields:

- `server`: MCP server name, required.
- `uri`: resource URI, required, usually from `list_mcp_resources`.

Legacy generic `MCPToolCall` and `mcpToolCall` names are not current loaded tool names; they map here for compatibility.
