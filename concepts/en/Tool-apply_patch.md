# apply_patch

`apply_patch` is Codex's structured file-editing tool. It accepts a freeform patch, not JSON.

Use it to:

- Add, delete, or update repository files.
- Keep edits reviewable instead of writing files through shell redirection.
- Express focused local changes precisely.

Legacy `FileChange` and `fileChange` entries map to this tool.
